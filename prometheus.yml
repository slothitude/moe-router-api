# Prometheus configuration for MoE Router API

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'moe-router-api'
    static_configs:
      - targets: ['moe-router:8000']
    metrics_path: '/api/v1/metrics/prometheus'
